goze:
  server:
    read-timeout:
    address:
    max-header-bytes:
    read-header-timeout:
    idle-timeout:
    write-timeout:
  cache:
    redis:
      connect-timeout:
      password:
      address:
      read-timeout:
      db:
      write-timeout:
      network:
  balancer:
    wrr:
      client:
        timeout:
    rule:
  micro-service:
    address:
    enable-server:
    name:
    enable-client:
    weight:
  sql:
    datasource:
    driver: